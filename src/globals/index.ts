import { homeConfig } from '@/globals/home';
import { networkConfig } from '@/globals/network';
import { promotionConfig } from '@/globals/promotion/promotion';
import { institutesConfig } from '@/globals/promotion/institutes';
import { instituteDetailConfig } from '@/globals/promotion/instituteDetail';
import { earlyCareerAwardConfig } from '@/globals/promotion/earlyCareerAward';
import { activitiesConfig } from '@/globals/activities/activities';

const globals = [
  homeConfig,
  networkConfig,
  promotionConfig,
  institutesConfig,
  instituteDetailConfig,
  earlyCareerAwardConfig,
  activitiesConfig,
];

export default globals;
